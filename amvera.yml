meta:
  environment: node
  toolchain:
    name: npm
    version: 18

build:
  additionalCommands: |
    npm install
    npm rebuild sqlite3

run:
  scriptName: server.js
  containerPort: 3000
  persistenceMount: /data